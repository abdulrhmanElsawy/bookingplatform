{
  "name": "growth-world",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "overrides": {
    "@types/express": "4.17.21",
    "@types/express-serve-static-core": "4.19.5"
  },
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "build:server": "npm run build -w shared && npm run build -w server",
    "start:prod": "npm run build:server && npm run start -w server",
    "test": "npm run test -w shared && npm run test -w client && npm run test -w server",
    "lint": "npm run lint -w client",
    "seed:dev": "npm run seed:dev -w server"
  }
}
